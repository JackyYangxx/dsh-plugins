# 全局规则（Global Rules）

本文件是 dsh-plugins 仓库对**所有 agent 会话**（含子代理）的强制约束。任何 agent 在仓库内工作前必须先读本文件。

## 进程与资源边界

1. **不允许 kill 当前会话之外的其他进程。**
   - 只能中断/停止本会话自己启动的子代理、后台任务或进程（interrupt_agent / job_kill 仅限本会话范围内）。
   - 不得 kill、终止或以任何方式干预其他会话、其他用户进程、系统服务或共享进程。
   - 若发现异常进程需要处理，如实上报，不擅自 kill。

## 其他约束

2. 不发布/不推送未经用户授权的包或分支；npm 发布必须经用户明确同意（当前：暂不发布 npm，本地使用）。
3. 不修改仓库外的文件（会话临时区除外）；不覆盖用户明确排除的配置。
4. 子代理执行类任务若长时间无进展（无文件改动、无提交），应如实报告并交由协调者决策，不无限重试。
5. 所有跨会话协作经协调者（主会话）路由，不直接干预其他会话。
